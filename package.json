{
  "name": "allotmentplotter",
  "version": "1.0.0",
  "description": "Allotment planning app",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --files '**/*.css, **/*.html, **/*.js, !node_modules/**/*' --directory --port 7777",
    "sass": "node-sass src/scss/main.scss --output dist/css --source-map-embed --source-map-contents --output-style compressed",
    "watch:sass": "npm run sass -- --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "d3": "^5.4.0"
  },
  "devDependencies": {
    "node-sass": "^4.9.0"
  }
}
